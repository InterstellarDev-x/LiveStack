# Copy to .env and adjust as needed. Read by docker-compose for variable
# substitution (${VAR} references in docker-compose.yml).
POSTGRES_PASSWORD=mysecretpassword
JWT_SECRET=change-me


OPENAI_API_KEY=your-api-key
OPENAI_MODEL=gpt-5-mini
