[package]
name = "consumer"
version = "0.1.0"
edition = "2024"

[dependencies]
curl = { version = "0.4", features = ["static-curl"] }
dotenvy = "0.15"
messaging = {path = "../messaging"}
serde = "1.0.228"
store = { path = "../store" }
tokio = { version = "1", features = ["full"] }
